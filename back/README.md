# AREA IFTT - Backend (base)

## Démarrage rapide

1. Installer les dépendances:
   - `pip install -r requirements.txt`
2. Lancer le serveur:
   - `uvicorn app.main:app --reload`
3. Tester:
   - `GET /health`
